 version: 2.1

 orbs:
  win: circleci/windows@2.2.0

 jobs:
   build:
     executor: win/default     
    
     steps:
       - checkout
       - run: choco install unity
       - run:
           name: "Building Unity app"
           command: build.bat
           shell: cmd.exe
